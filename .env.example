# Required for the AI paths. Without it the app still runs: the receipt flow
# returns a fixed sample bill and categorization falls back to keywords.
ANTHROPIC_API_KEY=

# Not wired up yet — lib/store.ts is still in-memory. These are here so the
# Supabase swap has an obvious home. See supabase/schema.sql.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
